function doubleInteger(i: number) {
  // `i` will be an integer
  // double it and return it.
  return i;
}

function isNumberEven(i: number) {
  // `i` will be an integer
  // return true if it's even
  // and false if it isn't.
  return i;
}

function getFileExtension(i: string) {
  // `i` will be a string,
  // but it may not have a file extension.
  // return the file extension (with no period)
  // if it has one, otherwise false
  // export const getFileExtension = i => i.includes('.') ? i.split('.').pop() : false;
  return 'string';
}

function longestString(i: Array<any>) {
  // `i` will be an array.
  // return the longest string in the array
  return 'string';
}

function arraySum(i: Array<any>) {
  // i will be an array, containing
  // integers, strings and/or arrays like itself.
  // Sum all the integers you find, anywhere in
  // the nest of arrays.
  return 1;
}

export const initialCode = [
  doubleInteger,
  isNumberEven,
  getFileExtension,
  longestString,
  arraySum,
];
